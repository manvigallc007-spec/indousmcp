# Caddy auto-provisions a free Let's Encrypt TLS cert for $DOMAIN and reverse-proxies
# HTTPS traffic to the MCP server container. Requires a domain pointed at the VPS IP.
{$DOMAIN} {
	reverse_proxy server:8000
}
